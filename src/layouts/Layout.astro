---
import Head from '@components/Head.astro'
import Navbar from '@components/Navbar.astro'
import '@styles/global.css'

interface Props {
	title?: string
}

const { title = 'Drix - Portfolio' } = Astro.props
---

<html lang="en">
	<Head {title} />
	<body class="font-dm-sans bg-black/90 text-gray-100">
		<div class="m-auto flex flex-col max-w-3xl">
			<Navbar />
			<slot />
		</div>
	</body>
</html>
